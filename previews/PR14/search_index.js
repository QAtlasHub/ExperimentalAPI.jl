var documenterSearchIndex = {"docs":
[{"category":"section","location":"api-checks/#API-—-checking-and-releasing","page":"Checking and releasing","text":"The audit (Checking), the coverage join, the mark's exit, and the release layer (Release decisions).","title":"API — checking and releasing"},{"category":"type","location":"api-checks/#ExperimentalAPI.Audit","page":"Checking and releasing","text":"Audit\n\nWhat audit found. The name fields are sorted Vector{Symbol}, and every name in surface appears in exactly one of foreign, documented, unaccounted or declared-and-not-documented — the partition partition_holds checks.\n\nfield \nmod the module audited\nsurface names(m) — exported or public\nforeign public here, but bound in another package; not this module's to declare\ndocumented has a docstring\ndeclared has an @experimental mark\nundocumented no docstring, mark or not\nunaccounted neither — the finding\ndangling marked experimental but not public; a mark that promises nothing\ntracking name → where its shape is being decided, for the marks that say\ncontributed_methods methods this module wrote on other modules' generics\nundocumented_methods of those, the ones with no docstring\nunaccounted_methods of those, the ones with neither a docstring nor a mark\nextensions this module's loaded package extensions, which are audited separately\n\nunaccounted is the one a test asserts is empty. dangling needs no external oracle to be wrong: it is the module contradicting itself.\n\n\n\n\n\n","title":"ExperimentalAPI.Audit"},{"category":"method","location":"api-checks/#ExperimentalAPI.aqua_compatible_names-Tuple{Module}","page":"Checking and releasing","text":"aqua_compatible_names(m::Module) -> Vector{Symbol}\n\nThe names Aqua.test_undocumented_names reports and this package's audit accounts for.\n\nThe two tools ask different questions, and both are legitimate. Aqua's is two-valued: a public name has a docstring or it fails. audit has a third answer — a mark — so a name that is declared unsettled is still undocumented but is not unaccounted.\n\nThis is the difference, computed, so a project running both can see exactly which names it would have to argue about. Empty means the two agree, which is the state a package should be aiming for: a mark records that a shape is unsettled, and that is never a reason to say nothing about what the name does.\n\n\n\n\n\n","title":"ExperimentalAPI.aqua_compatible_names"},{"category":"method","location":"api-checks/#ExperimentalAPI.audit-Tuple{Module}","page":"Checking and releasing","text":"audit(m::Module; methods = true) -> Audit\n\nReport the public names of m that are missing prose, a mark, or both — and the methods it contributed to other modules' generics, which no name-level check can see.\n\njulia> audit(Pinax).undocumented\n15-element Vector{Symbol}:\n :completeness_overview\n :dump_test_report\n ⋮\n\nA mark is not a substitute for a docstring. The two are independent accounts of a name and both are owed: the docstring says what it does, the mark says whether the shape is settled. A public name that carries a mark and no prose appears in undocumented exactly as one with neither does, and test_surface fails on it.\n\nfield what it holds\ndocumented has a docstring\ndeclared has a mark — overlaps documented, and is not an alternative to it\nundocumented no docstring, mark or not. This is the one test_surface asserts empty\nunaccounted neither account — a subset of undocumented, and the worst case\n\nAlso reports dangling: marks on names that are not public. That check needs no reference implementation to be right, because the module is disagreeing with itself.\n\nmethods = false skips the method-level search, which is the expensive half — it scans the generics of every loaded module. The name-level answer is unchanged by it.\n\nWhat this cannot see\n\nSignatures of its own names. A name that stays present while its arguments change is invisible here; compare_methods is the release-side answer.\nProse quality. A docstring exists or it does not; isdocumented reads no further.\nNames public only inside an extension, which is a separate module — reported in extensions and audited by passing it to audit in its own right. Avoidable altogether: declare the function and its docstring in the parent and let the extension add only the method, which is what this package does for test_surface.\n\nSee test_surface to run this as a test, and snapshot to carry the result into a release decision.\n\n\n\n\n\n","title":"ExperimentalAPI.audit"},{"category":"method","location":"api-checks/#ExperimentalAPI.contributed_methods-Tuple{Module}","page":"Checking and releasing","text":"contributed_methods(m::Module) -> Vector{Method}\n\nThe methods m defines on generics it does not own.\n\naudit's foreign bucket says \"this name is bound elsewhere, so documenting it is not our problem\". A method we wrote on such a name is the exact opposite: it is entirely our problem, and it is invisible to names(m).\n\n\n\n\n\n","title":"ExperimentalAPI.contributed_methods"},{"category":"method","location":"api-checks/#ExperimentalAPI.extends_base-Tuple{Method}","page":"Checking and releasing","text":"extends_base(mm::Method) -> Bool\n\nWhether mm extends a generic owned by Base or Core.\n\nThe line test_surface draws by default when it asks whether a contributed method is accounted for. A method on Base.show or Base.== implements a protocol whose documentation is Base's, and requiring a docstring on each of them trains a project to turn the whole check off. A method on another package's generic is a downstream extension only this package can describe — fetch_value(::Heisenberg, ::Energy) is surface in a way show(io, ::Audit) is not.\n\nStated as a rule about who owns the generic, not as a list of names: a list of interface functions is not closed under the ones Julia adds next.\n\n\n\n\n\n","title":"ExperimentalAPI.extends_base"},{"category":"method","location":"api-checks/#ExperimentalAPI.isdocumented-Tuple{Module, Symbol}","page":"Checking and releasing","text":"isdocumented(m::Module, name::Symbol) -> Bool\nisdocumented(m::Method) -> Bool\n\nWhether name, or the method m, has a docstring.\n\nA re-exported name counts: the lookup follows the binding to the module the name actually comes from, so a package that puts a dependency's name on its own surface is not asked to re-document it. audit still reports those separately as foreign, because who owns a name and who documented it are different questions.\n\nDocstrings are keyed by signature, so the method form is answerable and is not the same question: a documented generic with an undocumented method is normal, and for a package whose surface is methods on somebody else's generic it is the only question there is.\n\nThis answers whether prose exists, never whether it is any good. A docstring reading \"TODO\" is documented as far as this package is concerned.\n\n\n\n\n\n","title":"ExperimentalAPI.isdocumented"},{"category":"method","location":"api-checks/#ExperimentalAPI.own_methods-Tuple{Module}","page":"Checking and releasing","text":"own_methods(m::Module) -> Vector{Method}\n\nEvery method m defines, on its own generics and on other modules' alike.\n\nJulia indexes methods by generic, not by module, so this is a search: the generics bound in m (including the ones it imported to extend), the generics m's own marks name, and the exported-or-public names of every loaded module. That last set is what catches Base.show(io, ::Widget), written with a qualified name that leaves no binding in m.\n\nnote: The one gap, stated\nA method contributed to a generic that is neither bound in m nor exported-or-public anywhere — Base.SomeInternal.f(::Widget) = … — is not found. It is also not part of any surface anyone can be told about, which is why the search stops there rather than walking every binding of every loaded module.\n\n\n\n\n\n","title":"ExperimentalAPI.own_methods"},{"category":"method","location":"api-checks/#ExperimentalAPI.partition_holds-Tuple{ExperimentalAPI.Audit}","page":"Checking and releasing","text":"partition_holds(a::Audit) -> Bool\n\nWhether every public name landed in exactly one bucket.\n\nforeign, documented, unaccounted, and the declared-but-undocumented remainder must cover surface exactly once. The invariant is stated in Audit and is easy to break from the outside — every new field is one more thing a reader assumes partitions the surface — so it is checkable rather than only written down.\n\n\n\n\n\n","title":"ExperimentalAPI.partition_holds"},{"category":"method","location":"api-checks/#ExperimentalAPI.stable-Tuple{Module}","page":"Checking and releasing","text":"stable(m::Module) -> Vector{Symbol}\n\nThe public surface of m minus the names that are wholly @experimental — the names whose removal or renaming is a breaking change.\n\nThe covenant, in other words. snapshot writes this down so two releases can be compared; isbreaking is what reads the comparison.\n\n\"Wholly\" is the load-bearing word. A name is out of the covenant when a whole-name declaration covers it, or when every method behind it is marked. A name with four methods of which one is marked stays in — the author declared one dispatch path unsettled, which is not a licence to remove the name. See stable_methods for the finer unit.\n\n\n\n\n\n","title":"ExperimentalAPI.stable"},{"category":"method","location":"api-checks/#ExperimentalAPI.stable_methods-Tuple{Module}","page":"Checking and releasing","text":"stable_methods(m::Module) -> Vector{Method}\n\nThe methods m defines that carry no mark — the covenant at the unit a call site actually reaches.\n\nThe name-level stable cannot see this: fetch(::Ising, ::Energy) and fetch(::Heisenberg, ::Energy) are one name and two promises. Covers both m's own generics and the methods it contributed to somebody else's; see contributed_methods.\n\n\n\n\n\n","title":"ExperimentalAPI.stable_methods"},{"category":"method","location":"api-checks/#ExperimentalAPI.surface-Tuple{Module}","page":"Checking and releasing","text":"surface(m::Module) -> Vector{Symbol}\n\nThe public surface of m: every name it exports or declares public, sorted, excluding the module's own name.\n\nThis is names(m), given a name — since Julia 1.11 that list is precisely \"exported or public\", which is the surface a stability claim has to be made about. Names reachable only as M.internal_thing are not here, and are nobody's promise.\n\n\n\n\n\n","title":"ExperimentalAPI.surface"},{"category":"method","location":"api-checks/#ExperimentalAPI.unaccounted_methods-Tuple{Module}","page":"Checking and releasing","text":"unaccounted_methods(m::Module) -> Vector{Method}\n\nThe methods m contributed to other modules' generics that carry neither a docstring nor a mark.\n\nThe method-level twin of audit(m).unaccounted, and the finding for a package whose surface is methods rather than names. A name-level audit reports unaccounted = [] for such a package while having looked at none of them.\n\n\n\n\n\n","title":"ExperimentalAPI.unaccounted_methods"},{"category":"type","location":"api-checks/#ExperimentalAPI.Verification","page":"Checking and releasing","text":"Verification\n\nHow much of one marked definition the current run exercised.\n\nfield \nmark the declaration\ncovered, total executable lines run, and executable lines in the definition\nfraction covered / total, 0.0 for a definition the run never entered, or missing\n\nmissing is not zero. A run without --code-coverage has nothing to say about how much of a definition ran, and reporting 0.0 there would flag every marked definition in every ordinary run.\n\nWhether it ran at all is a different question, and this package already answers it exactly: the probe. A marked definition whose flag never fired is 0.0 whatever the line counters say — which is not a refinement but a correction. Measured on 1.14.0-DEV.3115: --code-coverage now emits a counter for the definition line of a method nothing ever called, so a one-line definition comes back \"fully covered\" on the strength of having been defined. Up to 1.12 that line had no counter at all, so the two versions disagree about the same file, and only one of them can be read as \"the suite ran this\".\n\n\n\n\n\n","title":"ExperimentalAPI.Verification"},{"category":"method","location":"api-checks/#ExperimentalAPI.coverage-Tuple{Module, Symbol}","page":"Checking and releasing","text":"coverage(m::Module, name::Symbol) -> Union{Float64,Missing}\n\nThe fraction of name's definition that this run executed, or missing if coverage is off.\n\nPartial coverage comes back partial: a definition whose error branch is never taken is not verified, and reporting it as 1.0 would be the false pass this whole package exists to remove.\n\n\n\n\n\n","title":"ExperimentalAPI.coverage"},{"category":"method","location":"api-checks/#ExperimentalAPI.coverage_enabled-Tuple{}","page":"Checking and releasing","text":"coverage_enabled() -> Bool\n\nWhether this process was started with --code-coverage.\n\nThe question coverage has to ask before reporting a number: without it there are no counts at all, and the honest answer is missing.\n\n\n\n\n\n","title":"ExperimentalAPI.coverage_enabled"},{"category":"method","location":"api-checks/#ExperimentalAPI.flush_coverage-Tuple{}","page":"Checking and releasing","text":"flush_coverage() -> Union{Dict,Nothing}\n\nWrite this process's coverage counters out and read them back, or nothing if coverage is off.\n\nJulia writes them at exit, which is too late for a test to assert on. The same C entry point the exit hook uses is called here instead, into a temporary file that is read and removed.\n\n\n\n\n\n","title":"ExperimentalAPI.flush_coverage"},{"category":"method","location":"api-checks/#ExperimentalAPI.stale_marks-Tuple{Module}","page":"Checking and releasing","text":"stale_marks(m::Module) -> Vector{Mark}\n\nThe marks whose recorded line no longer holds a @experimental declaration.\n\nA mark records where it was written. An edit above it moves the code and not the record, and every join keyed on that line — coverage here, and anything downstream reading mk.file/mk.line — then describes the wrong lines silently. Re-loading the module fixes it, which is why this reports rather than repairs: a stale mark means the module on disk and the module in memory differ.\n\n\n\n\n\n","title":"ExperimentalAPI.stale_marks"},{"category":"method","location":"api-checks/#ExperimentalAPI.unverified-Tuple{Module}","page":"Checking and releasing","text":"unverified(m::Module) -> Vector{Mark}\n\nThe marks whose definitions this run never executed at all.\n\nThe worst case, and the one worth a separate verb: code that is both unvalidated and untested.\n\nDoes not need --code-coverage. The signal is the probe the mark already emits, which is exact and costs nothing; coverage adds the partial fraction, which is a different question. A mark on a struct, a const or a name list carries no probe and is not listed — there is nothing to enter.\n\n\n\n\n\n","title":"ExperimentalAPI.unverified"},{"category":"method","location":"api-checks/#ExperimentalAPI.verification-Tuple{Module}","page":"Checking and releasing","text":"verification(m::Module) -> Vector{Verification}\n\nOne Verification per mark in m, sorted by name.\n\nData, not a printout — the same convention audit follows. Flushes the process's coverage counters once and joins them against every mark, so asking about twenty marks costs what asking about one does.\n\n\n\n\n\n","title":"ExperimentalAPI.verification"},{"category":"method","location":"api-checks/#ExperimentalAPI.age-Tuple{ExperimentalAPI.Mark, VersionNumber}","page":"Checking and releasing","text":"age(m::Module, name::Symbol, current::VersionNumber) -> Union{Int,Missing}\nage(mk::Mark, current::VersionNumber) -> Union{Int,Missing}\n\nHow many breaking releases the mark has been standing for, or missing if it records no since.\n\nCounted on the axis a version bump is breaking along, which under Julia's 0.x convention is the minor component and after 1.0 the major one. since = v\"0.1.0\" seen from v\"0.9.0\" is 8.\n\nsince exists so a mark cannot quietly become permanent. This is the function that reads it; see stale_since for the list form a CI job asks for.\n\n\n\n\n\n","title":"ExperimentalAPI.age"},{"category":"method","location":"api-checks/#ExperimentalAPI.exceeds_mark_cap-Tuple{Module, Int64}","page":"Checking and releasing","text":"exceeds_mark_cap(m::Module, cap::Int) -> Bool\n\nWhether m carries more than cap marks.\n\nThe ratchet, in the shape test_surface's skip already has: a number checked into the repository that can be lowered and not raised. It is a count and not a list because the list is experimental(m), and a project that wants the finer gate should assert on that instead.\n\n\n\n\n\n","title":"ExperimentalAPI.exceeds_mark_cap"},{"category":"method","location":"api-checks/#ExperimentalAPI.promotable-Tuple{Module}","page":"Checking and releasing","text":"promotable(m::Module) -> Vector{Mark}\n\nEvery mark in m whose exit condition is met — the work item list, in the direction of done.\n\n\n\n\n\n","title":"ExperimentalAPI.promotable"},{"category":"method","location":"api-checks/#ExperimentalAPI.ready_to_promote-Tuple{ExperimentalAPI.Mark}","page":"Checking and releasing","text":"ready_to_promote(m::Module, name::Symbol) -> Bool\nready_to_promote(mk::Mark) -> Bool\n\nWhether the declaration's own exit condition is met.\n\nNot \"is this marked\" but \"may this stop being marked\", answered by the thing that knows: the until= predicate the author wrote next to the reason.\n\n@experimental(\n    \"no reference value yet\",\n    since = v\"0.1.0\",\n    tracking = \"https://github.com/org/Pkg.jl/issues/12\",\n    until = () -> isfile(joinpath(@__DIR__, \"..\", \"test\", \"refs\", \"energy.toml\")),\n    energy(β) = 2β,\n)\n\nfalse for a mark with no until=, always — a mark whose exit was never written down cannot be retired mechanically, and reporting it \"ready\" because it happens to be covered by a test would be inventing a criterion the author did not state. Those marks are reported separately by marks_without_exit rather than being quietly lumped in with the ones still standing.\n\nA predicate that throws counts as not ready: an exit condition that cannot be evaluated has not been met.\n\n\n\n\n\n","title":"ExperimentalAPI.ready_to_promote"},{"category":"method","location":"api-checks/#ExperimentalAPI.stale_since-Tuple{Module, VersionNumber}","page":"Checking and releasing","text":"stale_since(m::Module, current::VersionNumber; releases::Int = 2) -> Vector{Mark}\n\nThe marks that have been standing for releases breaking releases or more.\n\nThe report a CI job turns into a nag. Marks with no since are not listed here — they are a different finding, and marks_without_exit plus a since-less mark is what a package that never intended to retire anything looks like.\n\n\n\n\n\n","title":"ExperimentalAPI.stale_since"},{"category":"method","location":"api-checks/#ExperimentalAPI.docstring_note-Tuple{ExperimentalAPI.Mark}","page":"Checking and releasing","text":"docstring_note(m::Module, name::Symbol) -> Union{String,Nothing}\ndocstring_note(mk::Mark) -> String\n\nThe admonition a docs build should render above name's docstring, or nothing if the name is settled.\n\nMarkdown, in Documenter's !!! warning form, carrying the reason, the version it has been unsettled since, and the tracking link — the three things a reader needs and the author has already written once:\n\njulia> println(docstring_note(MyPkg, :provisional))\n!!! warning \"Experimental\"\n    the r,s branch has no reference value\n\n    Unsettled since v0.2.0. Tracking: <https://example.invalid/issues/9>.\n\nnothing for a settled name is the point: a renderer that annotates everything says nothing. The Documenter extension turns this into an @experimental block; nothing stops a project splicing it in itself.\n\n\n\n\n\n","title":"ExperimentalAPI.docstring_note"},{"category":"method","location":"api-checks/#ExperimentalAPI.marks_markdown-Tuple{Module}","page":"Checking and releasing","text":"marks_markdown(m::Module) -> String\n\nEvery mark in m as one markdown block, for a docs page that wants the list in one place.\n\nSorted by name, with the reason, the version and the tracking link. A module with no marks renders a sentence saying so rather than nothing, because \"this page is empty\" and \"this build failed to find anything\" look identical otherwise.\n\nDeliberately heading-free. A docs builder registers heading anchors in an earlier pass than the one that expands a block like this, so a heading spliced in here arrives after the pass that was supposed to see it — Documenter's HTML writer asserts on exactly that.\n\n\n\n\n\n","title":"ExperimentalAPI.marks_markdown"},{"category":"constant","location":"api-checks/#ExperimentalAPI.compare_methods_sees_keywords","page":"Checking and releasing","text":"compare_methods_sees_keywords\n\ntrue. A signature key carries the keyword names a method declares, so adding, removing or renaming one moves the key and compare_methods reports it.\n\nStated as a constant rather than left to be discovered, because the neighbouring limit is real and has to be stated with it: keyword defaults live in the method body and are invisible here, so changing tol = 1e-8 to tol = 1e-6 moves nothing. Names yes, defaults no.\n\n\n\n\n\n","title":"ExperimentalAPI.compare_methods_sees_keywords"},{"category":"type","location":"api-checks/#ExperimentalAPI.Diff","page":"Checking and releasing","text":"Diff\n\nThe result of compare: how a module's public surface moved between two snapshots.\n\nfield breaking? \nremoved_stable yes a settled name is gone\ndemoted yes a settled name is now called experimental — a promise withdrawn\nremoved_experimental no this is what marking a name buys\npromoted no experimental → settled; the direction this is all for\nadded_stable no \nadded_experimental no \n\nisbreaking is the one-bit reading of the first two rows.\n\n\n\n\n\n","title":"ExperimentalAPI.Diff"},{"category":"type","location":"api-checks/#ExperimentalAPI.MethodDiff","page":"Checking and releasing","text":"MethodDiff\n\nDiff at method granularity — the same six buckets, keyed by signature rather than by name, as produced by compare_methods.\n\nA signature key carries the argument types and the keyword names, so a method whose arguments or keywords changed reads as one key removed and another added. That is the blind spot compare admits to and this closes; see compare_methods_sees_keywords for the part that stays open.\n\n\n\n\n\n","title":"ExperimentalAPI.MethodDiff"},{"category":"method","location":"api-checks/#ExperimentalAPI.compare-Tuple{AbstractDict, Module}","page":"Checking and releasing","text":"compare(old::AbstractDict, new::Union{Module,AbstractDict}) -> Diff\n\nSay mechanically what moved between two snapshots — and therefore whether the change is breaking.\n\nd = compare(read_snapshot(\"api.toml\"), MyPackage)\nisbreaking(d) && error(\"breaking: $(d.removed_stable) removed, $(d.demoted) demoted\")\n\nA removed name that was declared @experimental in old lands in removed_experimental and does not make the change breaking. That is the whole contract: the mark was the notice, and it was given in the source, at the definition, before the removal.\n\ndemoted — a name that was stable and is now marked experimental — counts as breaking. Retroactively withdrawing a promise is a change to what callers were told, not a correction of it.\n\nwarning: Names, not signatures\nThis compares name sets. A name present in both snapshots whose method signature, return type or keyword arguments changed is a breaking change compare cannot see. compare_methods is the finer instrument; read this one as a floor on breakage, never as a clearance.\n\n\n\n\n\n","title":"ExperimentalAPI.compare"},{"category":"method","location":"api-checks/#ExperimentalAPI.compare_methods-Tuple{AbstractDict, Module}","page":"Checking and releasing","text":"compare_methods(old::AbstractDict, new::Union{Module,AbstractDict}) -> MethodDiff\n\ncompare at method granularity: what moved between two snapshots' stable_methods and experimental_methods.\n\nThe unit a call site actually reaches. fetch(::Ising, ::Energy) and fetch(::Heisenberg, ::Energy) are one name and two promises, and removing the second is breaking exactly when it was not marked.\n\nReads the same snapshot file compare does — the two keys mirror the name-level pair — so a repository commits one file and a release script asks it both questions.\n\n\n\n\n\n","title":"ExperimentalAPI.compare_methods"},{"category":"method","location":"api-checks/#ExperimentalAPI.isbreaking-Tuple{ExperimentalAPI.Diff}","page":"Checking and releasing","text":"isbreaking(d::Union{Diff,MethodDiff}) -> Bool\n\nWhether d removes a settled name or method, or demotes one — the two moves that break callers who were told the truth.\n\nThis is the release gate: a true here means the version bump is major (or, under Julia's 0.x convention, a minor bump). Because a diff reads names and signatures and not behaviour, false means no breakage of this kind was found, not nothing broke.\n\n\n\n\n\n","title":"ExperimentalAPI.isbreaking"},{"category":"method","location":"api-checks/#ExperimentalAPI.read_snapshot-Tuple{AbstractString}","page":"Checking and releasing","text":"read_snapshot(path::AbstractString) -> Dict{String,Any}\n\nRead a TOML file written by write_snapshot.\n\n\n\n\n\n","title":"ExperimentalAPI.read_snapshot"},{"category":"method","location":"api-checks/#ExperimentalAPI.snapshot-Tuple{Module}","page":"Checking and releasing","text":"snapshot(m::Module) -> Dict{String,Any}\n\nWrite down what m currently promises: its stable names and its experimental ones with their reasons, and the same split at method granularity.\n\nThe result is plain Dict/String/Vector data, so TOML.print accepts it as-is (write_snapshot does that). Take one at each release; hand the old one and the new module to compare.\n\nmodule = \"MyPackage\"\nversion = \"0.4.2\"\nstable = [\"adapt\", \"measure\"]\nstable_methods = [\"adapt(::Model, ::Grid)\", \"measure(::Model)\"]\n\n[experimental.render_report]\nreason = \"reads Test's internal result tree\"\ntracking = \"https://github.com/org/MyPackage.jl/issues/12\"\n\n[experimental_methods.\"fetch_value(::Heisenberg, ::Energy)\"]\nreason = \"numerically delicate; no reference value\"\n\nmethods = false writes the name half only, which is what a package with no foreign methods and a slow method search wants.\n\n\n\n\n\n","title":"ExperimentalAPI.snapshot"},{"category":"method","location":"api-checks/#ExperimentalAPI.stamp-Tuple{AbstractString, Any}","page":"Checking and releasing","text":"stamp(path::AbstractString, f) -> String\nstamp(path::AbstractString, r::Record) -> String\n\nRun f, and write next to its result a record of which unvalidated code paths produced it.\n\nThe end state this package is for: a figure's directory says what the number came out of, in plain TOML that a reader a year later can open without resolving the package that wrote it.\n\nstamp(\"figures/energy_sweep.provenance.toml\") do\n    sweep(model; βs = 0.05:0.05:2.0)\nend\n\nReturns path. The file carries every mark the run entered, its reason, how often it was entered, and the versions of the packages the marks came from — see stamp_versions.\n\n\n\n\n\n","title":"ExperimentalAPI.stamp"},{"category":"method","location":"api-checks/#ExperimentalAPI.stamp_versions-Tuple{}","page":"Checking and releasing","text":"stamp_versions() -> Dict{String,Any}\n\nThe version of every loaded package, for stamp to write beside a result.\n\nenergy being experimental in v0.3 says nothing about v0.9, so a provenance record that names marks without naming versions describes a state nobody can get back to.\n\n\n\n\n\n","title":"ExperimentalAPI.stamp_versions"},{"category":"method","location":"api-checks/#ExperimentalAPI.write_snapshot-Tuple{AbstractString, Module}","page":"Checking and releasing","text":"write_snapshot(path::AbstractString, m::Module) -> String\n\nsnapshot m and write it to path as TOML. Returns path.\n\nCommitting this file at each release is what gives the next release something to compare against; a repository with no committed snapshot can be told what is experimental now, but not what changed.\n\n\n\n\n\n","title":"ExperimentalAPI.write_snapshot"},{"category":"module","location":"api-checks/#ExperimentalAPI.ExperimentalAPI","page":"Checking and releasing","text":"ExperimentalAPI\n\nSay, at the definition site, that a name is not settled yet — and find out, without asking, when a run went through it.\n\npublic decides who may call a name. Whether the name is finished is the orthogonal question, and the place it is usually answered is a sentence in a docstring that no tool reads. That matters most where it is least visible: a long numerical run finishes, hands back a number, and nothing in the result says which of the code paths behind it had never been validated.\n\nusing ExperimentalAPI\n\n@experimental \"convergence not established below β ≈ 0.1\" energy(m::Model) = m.β * correction(m)\n\nThe five questions this answers\n\nquestion the call\ndid this run go through unvalidated code? entered() — and the summary at exit says so anyway\nhow often, by which paths, and how much of the run? record(f)\ndoes this caller depend on something unvalidated, without naming it? reach(f, Tuple{…})\nwhat is unfinished here, and which public names are undescribed? experimental(M), audit(M)\nis dropping this name breaking? compare(old_snapshot, M) — see isbreaking\n\nThe first is the reason to have any of it. It is asked after a run, about the run, by somebody who is not the author — which is exactly the question a docstring is structurally unable to answer.\n\nThree layers, and what each costs\n\nDetection is on always and costs one short-circuit read in the body: measured at 1.03x on one thread and 0.985x on eight, for 10M calls of a numeric body — see overhead_when_detecting. A flag written once and read thereafter stops dirtying its cache line, which a counter (3.76x at eight threads, and losing 40% of its increments to races unless atomic) does not.\nRecording (record) counts, captures call paths and attributes time. It costs something, which is why it is a call and not a default, and each record reports its own overhead.\nAnalysis (reach) is static and answers about code rather than about a run. Its answer is three-valued: :depends, :clean, and :unknown for a call site it could not pin to a method. Reporting :unknown as :clean is not a weaker claim, it is a false one.\n\nOnly a definition with a body carries a flag. A mark written as a name list, or attached to a struct, a const or a module, is a declaration — queryable, audited and analysable, but not observed at run time. See @experimental for the form-by-form table.\n\nWhat this is not\n\nNot visibility. public (Julia 1.11) already decides who may call a name. A name can be public and experimental, or public and settled; those are independent axes.\nNot deprecation. @deprecate points the other way — a settled name on its way out.\nNot type stability. Unrelated axis, different tooling.\nNot a documentation generator. The prose belongs to the author; audit only ever checks whether prose exists, never whether it is any good.\n\nScope of the check\n\naudit compares names(M) — exported and public names — against two independent accounts: a docstring, and a mark. They are not alternatives; the docstring is owed either way. Because names(M) cannot see a method a package contributed to somebody else's generic, the audit also reports contributed_methods — for a package whose surface is such methods, a clean name audit reports nothing while covering nothing.\n\nSee compare for the same limit on the release side, and compare_methods for the finer unit.\n\n\n\n\n\n","title":"ExperimentalAPI.ExperimentalAPI"},{"category":"function","location":"api-checks/#ExperimentalAPI.test_surface","page":"Checking and releasing","text":"test_surface(m::Module; skip = Symbol[], require_tracking = false, max_marks = nothing,\n             methods = true, outputlevel = 0) -> Audit\n\nAssert, as a @testset, that every public name of m has a docstring, that every mark applies to a name that is actually public, and that every method m contributed to another module's generic is accounted for.\n\nA mark is not an alternative to prose. @experimental records that a shape is unsettled, which is never a reason to say nothing about what the name does, so a marked-but-undocumented name fails this test exactly as an unmarked one does. There is no switch to turn that off; skip is the only escape, and it is per-name, visible, and can only shrink.\n\nAvailable once Test is loaded (it lives in a package extension, so ExperimentalAPI itself never pulls Test into a runtime dependency). Put it in runtests.jl:\n\nusing MyPackage, ExperimentalAPI, Test\n\nExperimentalAPI.test_surface(MyPackage)\n\nkeyword \nskip names allowed to have no docstring. A stale entry fails — a name that has since been documented or removed is reported, so the list can only shrink\nrequire_tracking every mark must say where its shape is being decided\nmax_marks the ratchet: a number in the repository that can be lowered and not raised\nmethods run the method-level half, which is the expensive one\n\nReturns the Audit on the normal return path whether the testset passed or not. outputlevel ≥ 1 also prints it.\n\n\n\n\n\n","title":"ExperimentalAPI.test_surface"},{"category":"section","location":"api/#API-—-declaring","page":"Declaring","text":"The mark itself, and reading marks back out. The rest of the surface is on API — observing and analysing and API — checking and releasing; the split is by source file, listed explicitly, so a new file with no page is a build failure rather than a silently missing section.","title":"API — declaring"},{"category":"section","location":"api/#Declared-unfinished","page":"Declaring","text":"Generated from the package's own marks at build time by this package's own Documenter extension, so it cannot go stale — and it is the same call any consumer would make:\n\nDiff — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nMethodDiff — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\ncompare — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\ncompare_methods — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nisbreaking — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nread_snapshot — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nsnapshot — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nstamp — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nwrite_snapshot — the snapshot schema is young: nothing has been released against it, and the method half was added after the name half, so a file written by one version may not be readable by the next\n\n*covers every method of the name · **no exit condition recorded***\n\nA docstring says what a name does. That block says whether it is finished. Nothing on this page is undocumented — the names above are documented and declared, which is the normal state for something that works but whose shape is still being argued about.","title":"Declared unfinished"},{"category":"type","location":"api/#ExperimentalAPI.Mark","page":"Declaring","text":"Mark\n\nOne @experimental declaration.\n\nreason is the field the whole design exists for: why a name is not settled is knowledge only the author has, so it travels with the mark rather than being reconstructed by a reader later. file/line point at the declaration, which is the definition site in the attached form.\n\nfield \nmod::Module the module that wrote the mark, and where it is stored\nname::Symbol the marked name; a macro is stored as Symbol(\"@foo\")\nreason::String why it is not settled\nsince::Union{VersionNumber,Nothing} version the name has been experimental since\ntracking::Union{String,Nothing} where the shape is being decided — an issue, PR, or URL\nfile::Symbol, line::Int where the declaration is written\nsig::Union{Type,Nothing} the signature it attached to, or nothing for a whole-name claim\nincludes_constructors::Bool a type's constructors are covered by its mark\nuntil::Union{Function,Nothing} the exit condition — see ready_to_promote\n\nsig is what makes the claim narrower than the name. A mark attached to energy(::Numerical) says the name has something unsettled about it — audit reads it that way — while reach resolves calls to a method and only reports the marked one. A mark with sig === nothing (a name list, a struct, a const) covers every method the name has.\n\nSee experimental to read them back, and mark to look one up by name.\n\n\n\n\n\n","title":"ExperimentalAPI.Mark"},{"category":"method","location":"api/#ExperimentalAPI.Mark-Tuple{Module, Symbol, AbstractString}","page":"Declaring","text":"Mark(mod, name, reason; since, tracking, file, line, sig, includes_constructors, until)\n\nKeyword form, for the fields that are usually absent. The positional form takes them in the order of the table in Mark.\n\n\n\n\n\n","title":"ExperimentalAPI.Mark"},{"category":"method","location":"api/#ExperimentalAPI._newest_sig-Tuple{Any}","page":"Declaring","text":"_newest_sig(ftype) -> Union{Type,Nothing}\n\nThe signature of the most recently defined method under ftype.\n\nCalled from the statement the macro emits immediately after the definition, so \"most recent\" is the method that definition just created. Reading it back from the method table is the only route: the macro sees ::Numerical as syntax, and building Tuple{typeof(f), Numerical} out of syntax would mean evaluating names in a module that may not have them yet.\n\nReturns nothing rather than throwing if the lookup fails — a mark that loses its signature is a wider claim, never a wrong one, and a package must not fail to load over it.\n\n\n\n\n\n","title":"ExperimentalAPI._newest_sig"},{"category":"method","location":"api/#ExperimentalAPI.isnamewide-Tuple{ExperimentalAPI.Mark}","page":"Declaring","text":"isnamewide(mk::Mark) -> Bool\n\nWhether mk claims the whole name rather than one signature.\n\ntrue for a name list, a struct, a const, a module — the forms that carry no signature. A mark attached to a definition knows which method it created and is therefore narrower; see stable for where the difference is load-bearing.\n\n\n\n\n\n","title":"ExperimentalAPI.isnamewide"},{"category":"macro","location":"api/#ExperimentalAPI.@experimental-Tuple","page":"Declaring","text":"@experimental \"reason\" definition\n@experimental \"reason\" name₁ name₂ …\n@experimental \"reason\" since=v\"0.4.0\" tracking=\"…\" until=(() -> …) definition\n\nDeclare that a public name is not settled, and say why.\n\nThe reason is required and comes first. Everything after it is either one definition or a list of names already defined elsewhere:\n\n# attached to the definition\n@experimental(\n    \"signature will be wrapped once the write-back refactor settles\",\n    function ingest(config; doc, kwargs...)\n        # ...\n    end,\n)\n\n# declared for names defined in an included file\n@experimental(\n    \"reads Test's internal result tree; not dogfooded in CI\",\n    render_test_report,\n    dump_test_report,\n    load_test_dump,\n)\n\nOptional keywords come between the reason and the subject:\n\nkeyword \nsince=v\"0.4.0\" the version the name has been unsettled since — read by age\ntracking=\"…\" the issue or PR where the shape is being decided\nuntil=() -> … the exit condition: what would discharge the reason. See ready_to_promote\n\ntracking is what turns a mark into something a reader can act on. until is what turns it into a work item: a mark with no stated exit can be added but never mechanically retired, and marks_without_exit reports those.\n\nAttaches to function, short-form f(x) = …, a callable object (c::C)(x) = …, a method on another module's generic Base.show(io, ::T) = …, struct, mutable struct, abstract type, primitive type, macro (recorded as Symbol(\"@name\")), const, plain assignment, and a definition wrapped in @inline, @noinline, @generated, @propagate_inbounds, @assume_effects or Base.@kwdef.\n\nAnything else — a module (which Julia requires as a direct top-level statement, so it cannot be wrapped), a definition produced by some other macro, a bare qualified name with no signature — is rejected with a message pointing at the name-list form, because guessing which name such an expression defines is exactly the kind of silence this package exists to remove.\n\nMarking is not visibility: a marked name still has to be exported or declared public to be part of the surface. A mark on a name that is neither is reported by audit as dangling.\n\nWhat the mark claims\n\nA mark attached to a definition records the signature it created, so the claim is about that method. A mark written as a name list, or attached to a struct or a const, carries no signature and covers the whole name.\n\nThe difference shows up in three places: reach only reports the marked method, stable keeps a name in the covenant until every method behind it is marked, and audit reads either as \"the author has said something about this name\".\n\nWhat gets observed\n\nA definition with a body — function and short-form f(x) = …, including parametric, callable-object and return-type-annotated signatures — also gets a flag in that body, so entered can report whether the run went through it. The flag is one short-circuit read: 1.03x on one thread and 0.985x on eight, measured over 10M calls of a numeric body.\n\nEvery other form is a declaration only — recorded, queryable and audited, but not observed:\n\nform observed?\nfunction f(x) … end, f(x) = …, f(x::T) where {T} = …, f(x)::R = …, (c::C)(x) = … yes\nBase.show(io::IO, ::T) = … — a method on a foreign generic yes\na name list (@experimental \"…\" a b c) no — those definitions are elsewhere and already compiled\nstruct, abstract type, primitive type, const, assignment no — nothing is entered\nmacro no\n@generated function no — the body returns an expression, so a probe there would be generated rather than run\nBase.@kwdef, @inline, @noinline and the other pass-through macros no\n\nOne flag per marked name, not per method: flags are name-keyed, so two methods of a marked name share one.\n\nnote: Top level only\nThe mark is stored in a const binding in the enclosing module, so @experimental belongs at module top level — the same place export and public go. Written inside a function body it is refused by Julia's lowering, at the line you wrote it on.\n\nSee also experimental, audit, stable, reach.\n\n\n\n\n\n","title":"ExperimentalAPI.@experimental"},{"category":"method","location":"api/#ExperimentalAPI.experimental-Tuple{Function}","page":"Declaring","text":"experimental(f) -> Vector{Mark}\n\nEvery mark anyone has written on a method of the generic f, across all loaded modules.\n\nThe fetch_value(::Heisenberg, ::Energy) case: a generic is defined upstream and extended by several packages, each of which may declare its own methods unsettled. The marks live in the modules that wrote the methods, so answering this means asking all of them — see marks_on, which is the same search stated as a verb about the callee rather than about ownership.\n\n\n\n\n\n","title":"ExperimentalAPI.experimental"},{"category":"method","location":"api/#ExperimentalAPI.experimental-Tuple{Module}","page":"Declaring","text":"experimental(m::Module; extensions = false) -> Vector{Mark}\n\nEvery declaration m has written with @experimental, sorted by name.\n\nThis is the query the marker exists to make possible — a tool asks the module, rather than a human reading docstrings. A module with no marks answers with an empty vector; it never errors for not having opted in.\n\nfor mk in experimental(MyPackage)\n    println(mk.name, \" — \", mk.reason)\nend\n\nIncludes marks on methods of other modules' generics, which m wrote and therefore owns: those carry a sig and a name that is not m's. extensions = true also walks m's package extensions, whose public names are part of the surface a user sees and are invisible to names(m).\n\nThe result is a fresh vector; mutating it does not change the module. Note that a mark does not imply the name is public — see audit's dangling.\n\n\n\n\n\n","title":"ExperimentalAPI.experimental"},{"category":"method","location":"api/#ExperimentalAPI.experimental_methods-Tuple{Module}","page":"Declaring","text":"experimental_methods(m::Module) -> Vector{Mark}\n\nThe subset of m's marks that carry a signature — the claims about a method rather than about a whole name.\n\nEvery attached form produces one, including @experimental \"…\" Base.show(io, ::T) = …, which is how a package declares a method it contributed to somebody else's generic unsettled. The mark is stored in the module that wrote the method, never in the module that owns the name: a package cannot carry claims its dependents invented, and the mark has to survive that package being reloaded.\n\n\n\n\n\n","title":"ExperimentalAPI.experimental_methods"},{"category":"method","location":"api/#ExperimentalAPI.isexperimental-Tuple{Module, Symbol}","page":"Declaring","text":"isexperimental(m::Module, name::Symbol) -> Bool\nisexperimental(m::Method) -> Bool\n\nWhether name in m, or the method m, is declared @experimental.\n\nThe one-bit form of mark, for a caller that only needs the verdict — for instance the mechanical statement that changing this name is not breaking. Does not consult docstrings and does not consult visibility: it answers only whether a mark exists.\n\n\n\n\n\n","title":"ExperimentalAPI.isexperimental"},{"category":"method","location":"api/#ExperimentalAPI.mark-Tuple{Method}","page":"Declaring","text":"mark(m::Method) -> Union{Mark,Nothing}\n\nThe Mark covering the method m, or nothing.\n\nA method is covered by a mark on its exact signature, or by a whole-name mark on the generic it belongs to. The second is why a name list still says something about every method behind the name, and the first is why marking one dispatch path does not speak for its siblings.\n\n\n\n\n\n","title":"ExperimentalAPI.mark"},{"category":"method","location":"api/#ExperimentalAPI.mark-Tuple{Module, Symbol}","page":"Declaring","text":"mark(m::Module, name::Symbol) -> Union{Mark,Nothing}\n\nThe Mark on name in m, or nothing if it carries none.\n\nThis is how the reason gets to whoever needs it — an error message, a docs page, a release checklist:\n\nmk = mark(MyPackage, :render_report)\nmk === nothing || @warn \"not settled\" mk.reason mk.tracking\n\nA name may carry several marks when separate methods behind it were declared separately. The whole-name declaration is returned in preference to any of them, because it is the wider claim; marks returns all of them.\n\n\n\n\n\n","title":"ExperimentalAPI.mark"},{"category":"method","location":"api/#ExperimentalAPI.mark_method!-Tuple{Method, AbstractString}","page":"Declaring","text":"mark_method!(m::Method, reason::AbstractString; since, tracking, until) -> Mark\n\nDeclare one method unsettled, from outside its definition.\n\nThe imperative route to what @experimental \"…\" f(::T) = … does at the definition site. It is here for the cases the macro cannot reach: a method produced by another package's macro, a method generated in a loop, and a package adopting this on code it does not want to touch yet.\n\nThe mark is stored in the module that wrote the method, m.module, exactly where the macro would have put it — so it is queryable through experimental_methods and survives the same way.\n\nmark_method!(\n    which(fetch_value, Tuple{Heisenberg,Energy}),\n    \"numerically delicate; no reference value\";\n    tracking = \"https://github.com/org/Pkg.jl/issues/12\",\n)\n\nPrefer the macro. A mark that is not next to the definition is a mark the next person editing that definition will not see.\n\n\n\n\n\n","title":"ExperimentalAPI.mark_method!"},{"category":"method","location":"api/#ExperimentalAPI.marks-Tuple{Module, Symbol}","page":"Declaring","text":"marks(m::Module, name::Symbol) -> Vector{Mark}\n\nEvery mark m carries under name — the whole-name declaration if there is one, and one per separately declared method.\n\n\n\n\n\n","title":"ExperimentalAPI.marks"},{"category":"method","location":"api/#ExperimentalAPI.marks_on-Tuple{Any}","page":"Declaring","text":"marks_on(f) -> Vector{Mark}\nmarks_on(m::Method) -> Vector{Mark}\n\nEvery mark written on a method of f, or on m specifically, wherever it was written.\n\nThe cross-module search. experimental_methods is the ownership question — \"what has this module declared?\" — and this is the call-site question — \"has anyone said anything about what I am about to call?\". One name for both would leave a reader unable to tell which they got.\n\n\n\n\n\n","title":"ExperimentalAPI.marks_on"},{"category":"method","location":"api/#ExperimentalAPI.marks_without_exit-Tuple{Module}","page":"Declaring","text":"marks_without_exit(m::Module) -> Vector{Mark}\n\nThe marks that say why a name is unsettled but not what would settle it.\n\nA mark with no until= can be added and never mechanically retired — ready_to_promote can only answer false for it, forever. That is a decoration rather than a work item, so it is reported as its own finding rather than being folded into \"not ready\".\n\n\n\n\n\n","title":"ExperimentalAPI.marks_without_exit"},{"category":"method","location":"api/#ExperimentalAPI.package_extensions-Tuple{Module}","page":"Declaring","text":"package_extensions(m::Module) -> Vector{Module}\n\nEvery loaded package extension of m.\n\nAn extension is a separate module whose public names are part of the surface a user sees and are invisible to names(m). audit reports which of them are loaded rather than folding them in, because an extension that is not loaded is not missing — it is inapplicable.\n\n\n\n\n\n","title":"ExperimentalAPI.package_extensions"},{"category":"method","location":"api/#ExperimentalAPI.superseded_marks-Tuple{Module}","page":"Declaring","text":"superseded_marks(m::Module) -> Vector{Mark}\n\nThe declarations that were replaced by a later one on the same name and signature.\n\nRe-marking is last-write-wins, which is the right rule — a file included twice must not double its marks — but it is a rule that can lose a reason silently. Anything it drops is kept here, so \"the mark says something different from what I wrote\" is answerable rather than mysterious.\n\nEmpty for almost every module: nothing is recorded unless a mark actually replaced a different one.\n\n\n\n\n\n","title":"ExperimentalAPI.superseded_marks"},{"category":"section","location":"declaring/#Declaring","page":"Declaring","text":"","title":"Declaring"},{"category":"section","location":"declaring/#The-two-forms","page":"Declaring","text":"@experimental takes a reason — always first, never optional — and then either one definition or a list of names.\n\n# attached to the definition: the mark and the thing it describes cannot drift apart\n@experimental(\n    \"signature will be wrapped once the write-back refactor settles\",\n    function ingest(config; doc, kwargs...)\n        # ...\n    end,\n)\n\n# a list, for names an included file defines\n@experimental(\n    \"reads Test's internal result tree; not dogfooded in CI\",\n    render_test_report,\n    dump_test_report,\n    load_test_dump,\n)\n\nThe attached form is preferred where it fits, for the same reason a docstring goes above its function: a declaration two hundred lines from what it describes stops being true without anyone noticing.","title":"The two forms"},{"category":"section","location":"declaring/#The-reason-is-the-payload","page":"Declaring","text":"Why a name is not settled is knowledge only the author has. A reader can see that a signature is odd; they cannot see that it is odd because a refactor upstream has not landed. So the reason is required, and the mark carries it everywhere the name goes:\n\njulia> mark(Archeion, :ingest)\nArcheion.ingest — experimental\n  reason:   signature will be wrapped once the write-back refactor settles\n  since:    v0.1.4\n  tracking: https://github.com/QAtlasHub/Archeion.jl/issues/12\n  declared: /…/Archeion.jl/src/ingest.jl:153\n\nsince and tracking are optional and go between the reason and the subject:\n\n@experimental(\"export format is a guess until someone consumes it\",\n    since = v\"0.4.0\",\n    tracking = \"https://github.com/org/Pkg.jl/issues/12\",\n    registry_entry(x) = x)\n\ntracking is what turns a mark into something a reader can act on rather than merely be warned by: it names where the shape is being decided, so a downstream author can go and argue for the shape they need.","title":"The reason is the payload"},{"category":"section","location":"declaring/#What-it-attaches-to","page":"Declaring","text":"function, short-form f(x) = …, struct, mutable struct, abstract type, primitive type, macro (recorded as Symbol(\"@name\")), const, and plain assignment.\n\nAnything else is refused with a message naming the alternative, never guessed at:\n\njulia> @experimental \"why\" Base.sum(x::Int) = x\nERROR: ArgumentError: @experimental: `Base.sum` defines a name owned by another module,\nwhich is not part of this module's public surface\n\nThe refused cases and why:\n\n \nmodule M … end Julia requires it as a direct top-level statement, so it cannot be wrapped — use the name-list form\nBase.foo(x) = … adds a method to a name this module does not own; it is not on your surface\n@somemacro … the macro cannot know which name the expansion defines — name it explicitly\n\nGuessing in any of these cases would produce a mark on the wrong symbol, which is worse than no mark: audit would then report it as dangling and the author would be debugging this package instead of their own.","title":"What it attaches to"},{"category":"section","location":"declaring/#Docstrings-still-work","page":"Declaring","text":"A name can be documented and declared unfinished. The two accounts answer different questions, and nothing forces a choice between them:\n\n\"\"\"\n    ingest(config; doc, kwargs...)\n\nIngest `doc` into the registry described by `config`.\n\"\"\"\n@experimental(\n    \"signature will be wrapped once the write-back refactor settles\",\n    function ingest(config; doc, kwargs...)\n        # ...\n    end,\n)","title":"Docstrings still work"},{"category":"section","location":"declaring/#Marking-is-not-making-public","page":"Declaring","text":"A mark says nothing about visibility. A name still has to be exported or declared public to be part of the surface, and a mark on a name that is neither promises nothing to anyone — audit reports it as dangling:\n\nmodule M\nusing ExperimentalAPI\n@experimental \"not settled\" helper(x) = x   # never exported, never public\nend\n\njulia> audit(M).dangling\n1-element Vector{Symbol}:\n :helper\n\nThat check needs no reference to be right. The module is disagreeing with itself.","title":"Marking is not making public"},{"category":"section","location":"declaring/#Top-level-only","page":"Declaring","text":"The mark is stored in a const binding in the enclosing module, so @experimental belongs where export and public go. Inside a function body it will fail with Julia's own error about const in local scope.","title":"Top level only"},{"category":"section","location":"declaring/#Where-the-marks-live","page":"Declaring","text":"In the marked module, as M.__EXPERIMENTAL_API_MARKS__ — a plain Vector{Mark} you can look at. Not in a table inside ExperimentalAPI, because a table there would be written while your package is being precompiled and would not be part of the cache image your users load. It is the same reason Julia keeps docstrings in a per-module Docs.META.","title":"Where the marks live"},{"category":"section","location":"adopting/#Adopting-it","page":"Adopting it","text":"The interesting case is not a new package. It is a package that already has thirty public names and no idea which of them anybody meant.","title":"Adopting it"},{"category":"section","location":"adopting/#Start-by-looking","page":"Adopting it","text":"julia> using MyPackage, ExperimentalAPI\n\njulia> audit(MyPackage)\n\nTwo numbers matter. undocumented is the backlog. dangling should be zero on day one, because there are no marks yet.","title":"Start by looking"},{"category":"section","location":"adopting/#Turn-the-test-on-with-the-backlog-listed","page":"Adopting it","text":"ExperimentalAPI.test_surface(MyPackage; skip = [:legacy_one, :legacy_two, :legacy_three])\n\nGreen from the first commit, and the list is the honest statement of what is not covered — in the test file, where the next person to touch the package will see it.","title":"Turn the test on with the backlog listed"},{"category":"section","location":"adopting/#The-list-can-only-shrink","page":"Adopting it","text":"A stale skip entry fails the test. An entry that has since been documented, declared, or deleted is reported by name:\n\npublic surface of MyPackage: Test Failed\n  skip entry legacy_two is still needed\n\nThis is the part that makes an allowlist worth having. Without it, the usual outcome is a list that silently stops describing anything: names get documented, names get deleted, and a green suite comes to mean less every release while looking exactly the same. Failing on a stale entry means the only way the list changes is deliberately, and only downwards.","title":"The list can only shrink"},{"category":"section","location":"adopting/#Then,-for-each-name,-one-decision","page":"Adopting it","text":"For every name in the list, write the docstring — that part is not optional, and it is what deletes the skip entry. Then ask a second question: is the shape settled?\n\nsettled → nothing more to do;\nnot settled → say so, with the reason, in addition to the docstring:\n\n@experimental(\n    \"reads Test's internal result tree; not dogfooded in CI\",\n    render_test_report,\n    dump_test_report,\n    load_test_dump,\n)\n\nThe mark is not a substitute for the prose and never was: the docstring says what the name does, which is owed either way. What the mark adds is the thing a docstring cannot carry — a machine can read it, so a run that goes through the name says so, and dropping the name later is not a breaking change.","title":"Then, for each name, one decision"},{"category":"section","location":"adopting/#What-\"declare-it\"-is-worth-later","page":"Adopting it","text":"Once the marks exist, three things follow that did not before:\n\na user of your package can ask it, in code, which parts are not settled;\nyour release process can say mechanically that removing one of them is not breaking (Release decisions);\nthe reasons are in one place, so \"what still needs deciding here\" is a query rather than an archaeology exercise.","title":"What \"declare it\" is worth later"},{"category":"section","location":"adopting/#A-note-on-where-this-ends","page":"Adopting it","text":"The goal is not zero experimental names. A package with none is either finished or lying. The goal is zero undocumented names — every public name described, whether or not its shape has settled.","title":"A note on where this ends"},{"category":"section","location":"api-runtime/#API-—-observing-and-analysing","page":"Observing and analysing","text":"What a run went through (Observing), and what a caller could reach (Analysing).","title":"API — observing and analysing"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Entry","page":"Observing and analysing","text":"Entry\n\nOne marked definition that the current run entered, as reported by entered.\n\ncount is always nothing. The default layer knows whether a definition was entered, never how often — a per-call counter costs 3.76x on eight threads and loses 40% of its increments to races unless it is atomic. Counting is record's job, and it is opt-in for that reason.\n\n\n\n\n\n","title":"ExperimentalAPI.Entry"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Probe","page":"Observing and analysing","text":"Probe\n\nThe one-field flag @experimental puts in a marked body, and the counter record reads.\n\nNot part of the public surface — it is named rather than gensym'd only so that MyModule.__EXPERIMENTAL_API_ENTERED_energy__ is greppable when a query result surprises someone.\n\nentered is the whole default layer: read on every call, written on the first. The remaining fields are untouched unless a recording is open.\n\n\n\n\n\n","title":"ExperimentalAPI.Probe"},{"category":"method","location":"api-runtime/#ExperimentalAPI.detecting-Tuple{}","page":"Observing and analysing","text":"detecting() -> Bool\n\nWhether the summary at process exit is armed.\n\nOn by default, because a user who never asks is exactly the one who needs to be told. Set ENV[\"EXPERIMENTALAPI_SUMMARY\"] = \"0\" before using ExperimentalAPI to silence it; the atexit hook is registered at load time, so a later assignment has no effect.\n\nDetection itself is not a switch: the flag is in the definition and costs nothing to leave on. See overhead_when_detecting for the figure that justifies that sentence.\n\n\n\n\n\n","title":"ExperimentalAPI.detecting"},{"category":"method","location":"api-runtime/#ExperimentalAPI.entered-Tuple{Module}","page":"Observing and analysing","text":"entered(m::Module) -> Vector{Entry}\nentered() -> Vector{Entry}\n\nThe marked definitions this run has entered at least once.\n\nWithout an argument, every loaded module that carries marks. This is the question a docstring cannot answer: not \"is this name experimental\" but \"did the number I am about to publish come out of code nobody has validated\".\n\nA definition that was never called is absent, not reported with a count of zero.\n\njulia> MyPkg.energy(0.5);\n\njulia> entered()\n1-element Vector{ExperimentalAPI.Entry}:\n Entry(MyPkg.energy, \"convergence not established below β ≈ 0.1\")\n\nOnly definitions with a body are observed; see @experimental for which forms those are.\n\n\n\n\n\n","title":"ExperimentalAPI.entered"},{"category":"method","location":"api-runtime/#ExperimentalAPI.marked_modules-Tuple{}","page":"Observing and analysing","text":"marked_modules() -> Vector{Module}\n\nEvery loaded module that carries at least one @experimental mark.\n\nFound by walking the loaded packages rather than by a registry inside this package: a table here would be written while the marked package is precompiled, and so would be absent from its cache image. Same constraint that puts the marks themselves in the marked module.\n\n\n\n\n\n","title":"ExperimentalAPI.marked_modules"},{"category":"method","location":"api-runtime/#ExperimentalAPI.overhead_when_detecting-Tuple{}","page":"Observing and analysing","text":"overhead_when_detecting() -> Float64\n\nThe measured cost of the default layer, as a fraction of the unmarked body's run time.\n\n0.03. Not computed at call time: it is the figure from the measurement that chose this mechanism — 10M calls of a realistic numeric body on Julia 1.12.2, minimum of 7–9 trials, giving 1.03x on one thread and 0.985x on eight. The eight-thread figure is below one because a flag written once and read thereafter stops dirtying its cache line.\n\nIt is reported rather than re-measured because a wall-clock measurement on a shared CI runner is a flake generator, and because a number that moves with the machine cannot be the thing a caller plans against. record reports its own overhead per run, which is the opposite case: that one depends on how often the marked code was entered.\n\n\n\n\n\n","title":"ExperimentalAPI.overhead_when_detecting"},{"category":"method","location":"api-runtime/#ExperimentalAPI.probes-Tuple{Module}","page":"Observing and analysing","text":"probes(m::Module) -> Vector{Probe}\nprobes() -> Vector{Probe}\n\nEvery Probe a module carries — one per marked definition that has a body.\n\nThe recording layer arms and reads these; nothing else should need them. Exposed because record's cost model is only checkable by someone who can see how many probes there are.\n\n\n\n\n\n","title":"ExperimentalAPI.probes"},{"category":"function","location":"api-runtime/#ExperimentalAPI.summary_text","page":"Observing and analysing","text":"summary_text() -> String\nsummary_text(es::AbstractVector{Entry}) -> String\n\nWhat the exit summary prints, as a string. Empty when nothing marked was entered.\n\nCarries the reason, not just the name: the name tells a reader which line to look at, the reason tells them whether the result they are holding is affected.\n\n\n\n\n\n","title":"ExperimentalAPI.summary_text"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Attribution","page":"Observing and analysing","text":"Attribution\n\nOne marked definition's share of a profile buffer, as reported by attribute.\n\nsamples is a sample count, never a call count: inclusive is the fraction of samples with this definition anywhere on the stack, exclusive the fraction with it on top.\n\n\n\n\n\n","title":"ExperimentalAPI.Attribution"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Hit","page":"Observing and analysing","text":"Hit\n\nOne marked definition a record block entered, and what it cost.\n\nfield \nmod, name which marked definition\nreason carried into the record, so a reader a year later needs no source\ncount how many times it was entered, summed over threads\nmethod the method the mark attached to, when it attached to one\ncallers the distinct immediate callers seen\npaths the distinct call paths seen, innermost first, bounded\ninclusive, exclusive seconds, or missing when no timing backend was loaded\n\ncount is exact. paths is a bounded sample: a backtrace costs microseconds, so the recorder stops looking once it has seen enough, and a path that only occurs after the budget is spent is absent. inclusive counts time in anything this definition called; exclusive counts only time in the definition itself, which is what separates a marked wrapper over settled code from a marked kernel.\n\n\n\n\n\n","title":"ExperimentalAPI.Hit"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Record","page":"Observing and analysing","text":"Record\n\nWhat record observed: a Vector-like of Hit, plus what the run was.\n\nproperty \nenabled whether recording was actually on — an empty record means \"nothing was entered\", and that is a different statement from \"nothing was recorded\"\nslots thread slots the counters were sized for, at least Threads.maxthreadid()\nelapsed seconds the recorded call took\noverhead the recorder's estimated share of elapsed\nversions package versions the marks were read against\nsampled whether a timing backend produced inclusive/exclusive\n\nIndexing, iteration and == are the Hit vector's, so record(f) == [] reads the way it looks. The extra properties are why it is a type and not a plain vector: an empty Vector{Hit} cannot tell \"touched nothing\" from \"recording was off\", and those mean opposite things.\n\n\n\n\n\n","title":"ExperimentalAPI.Record"},{"category":"type","location":"api-runtime/#ExperimentalAPI.TimingBackend","page":"Observing and analysing","text":"TimingBackend\n\nHow record gets inclusive/exclusive time. The one implementation lives in this package's Profile extension; without it, timing is missing rather than zero.\n\n\n\n\n\n","title":"ExperimentalAPI.TimingBackend"},{"category":"method","location":"api-runtime/#ExperimentalAPI.assert_clean-Tuple{Any}","page":"Observing and analysing","text":"assert_clean(f; throw = true) -> Bool\n\nRun f and assert it entered nothing marked.\n\nThe gate. true when the run touched no marked definition; otherwise it throws, naming every mark it went through and why — or returns false if throw = false, which is what a caller doing its own reporting wants.\n\nassert_clean() do\n    publish(compute(model))\nend\n\n\n\n\n\n","title":"ExperimentalAPI.assert_clean"},{"category":"method","location":"api-runtime/#ExperimentalAPI.attribute-Tuple{Any}","page":"Observing and analysing","text":"attribute(data) -> Vector{Attribution}\n\nAttribute an existing profile buffer to marked definitions, after the fact.\n\nusing Profile\nProfile.@profile long_run()\nattribute(Profile.fetch())\n\nThe twelve-hour-run case: a job that was already profiled must not have to be run again to learn which of its time went through unvalidated code. What comes back is samples, not calls — which is why it is an Attribution and not a Hit. A sampling profiler cannot count entries, and a field called count holding a sample total would read as a measurement it did not make.\n\nnote: What sampling cannot see, and why counts exist\nA marked definition small enough to be inlined into its caller may leave no separately attributable samples at all: after inlining there is no frame to attribute them to, and the time is charged to whatever the optimiser left at that address. That is not a defect here, it is what a sampling profiler is. It is also the reason record's count is exact and comes from a counter rather than from samples.\n\n\n\n\n\n","title":"ExperimentalAPI.attribute"},{"category":"method","location":"api-runtime/#ExperimentalAPI.experimental_fraction-Tuple{ExperimentalAPI.Record}","page":"Observing and analysing","text":"experimental_fraction(r::Record) -> Union{Float64,Missing}\n\nThe share of the recorded run spent inside marked code, in [0, 1].\n\nmissing when no TimingBackend was loaded — a run whose time was never attributed has no fraction, and reporting 0.0 would say the opposite of what is known. Derived from the inclusive times, so it is time and not calls: one entry into a marked kernel that runs for a minute matters more than a million into a marked accessor.\n\n\n\n\n\n","title":"ExperimentalAPI.experimental_fraction"},{"category":"method","location":"api-runtime/#ExperimentalAPI.merge_records-Tuple{Any}","page":"Observing and analysing","text":"merge_records(rs) -> Record\n\nCombine records — from separate processes, separate workers, or separate blocks — into one.\n\nCounts add, paths and callers union, elapsed times add. Order-independent and associative: workers finish in whatever order they finish in, and provenance must not depend on that.\n\n\n\n\n\n","title":"ExperimentalAPI.merge_records"},{"category":"method","location":"api-runtime/#ExperimentalAPI.read_record-Tuple{AbstractString}","page":"Observing and analysing","text":"read_record(path::AbstractString) -> Record\n\nRead back a record written by write_record.\n\nThe method field of every Hit comes back nothing: a Method is not a thing a file can carry, and reconstructing one would mean claiming the code in this process is the code that produced the record.\n\n\n\n\n\n","title":"ExperimentalAPI.read_record"},{"category":"method","location":"api-runtime/#ExperimentalAPI.record-Tuple{Any}","page":"Observing and analysing","text":"record(f; paths = true, timing = true, with_profile = false, rethrow = true, maxdepth) -> Record\n\nRun f and report which marked definitions it entered, how often, and by which paths.\n\nr = record() do\n    simulate(model; steps = 10_000)\nend\nisempty(r) || @warn \"used unvalidated code\" [(h.name, h.count) for h in r]\n\nA definition that was never entered is absent, not reported with a count of zero: the record says what happened, and enumerating what did not is experimental's job.\n\nkeyword \npaths capture call paths. Bounded — see Hit — but still the expensive part\ntiming ask the TimingBackend for inclusive/exclusive. Ignored if none is loaded\nwith_profile leave whatever is already in the profile buffer alone instead of clearing it\nrethrow false returns the record for the part of f that ran instead of propagating\n\nNests: an inner record reports its own block, and the outer one still counts each entry once. Counts are exact under threads — per-thread counters sized by Threads.maxthreadid(), not nthreads(), because the interactive pool means a task can have a thread id above the worker count.\n\nnote: This costs something, which is why it is a call\nEvery marked body takes its write path while a recording is open. The record reports the recorder's estimated share of the elapsed time in overhead; overhead_when_detecting is the other number, and it is 3%.\n\n\n\n\n\n","title":"ExperimentalAPI.record"},{"category":"method","location":"api-runtime/#ExperimentalAPI.recording-Tuple{}","page":"Observing and analysing","text":"recording() -> Bool\n\nWhether a record block is open.\n\nfalse by default and outside record, which is the whole cost argument: detection is on always and free, counting happens only where somebody asked for it.\n\n\n\n\n\n","title":"ExperimentalAPI.recording"},{"category":"method","location":"api-runtime/#ExperimentalAPI.timing_backend-Tuple{}","page":"Observing and analysing","text":"timing_backend() -> TimingBackend\n\nThe loaded timing backend. NoTiming() until Profile is loaded, after which record reports seconds instead of missing.\n\n\n\n\n\n","title":"ExperimentalAPI.timing_backend"},{"category":"method","location":"api-runtime/#ExperimentalAPI.write_record-Tuple{AbstractString, ExperimentalAPI.Record}","page":"Observing and analysing","text":"write_record(path::AbstractString, r::Record) -> String\n\nWrite a record to TOML. Returns path.\n\nEvidence, not a printout: what a run went through belongs next to the result it produced, and it has to be readable by something that is not this package — a year later the package may not resolve. See stamp for the same idea aimed at a result file rather than at a record.\n\n\n\n\n\n","title":"ExperimentalAPI.write_record"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Reach","page":"Observing and analysing","text":"Reach\n\nWhat reach found.\n\nfield \nentry what was analysed\nreached the marked definitions proved reachable\nunresolved the call sites that could not be pinned to a method\nthrough_modules every module the walk went through\naffected_entries for a module or script entry: the public entry points that are not clean\nvisited how many distinct signatures were inferred\ntruncated whether a depth limit stopped the walk\n\nThere is deliberately no verdict field. A stored verdict makes :clean with a non-empty unresolved representable, and that state is the single thing this analysis must never report. verdict derives it instead, the way isbreaking derives its answer from a Diff.\n\n\n\n\n\n","title":"ExperimentalAPI.Reach"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Reached","page":"Observing and analysing","text":"Reached\n\nOne marked definition the analysis proved reachable from the entry point.\n\nmethod is the method that was resolved — nothing when the dependency is not a call, which is what a marked const is. path is the chain of names from the entry point down to it, so the reader learns which of their own code to distrust rather than only that something is wrong.\n\n\n\n\n\n","title":"ExperimentalAPI.Reached"},{"category":"type","location":"api-runtime/#ExperimentalAPI.Unresolved","page":"Observing and analysing","text":"Unresolved\n\nOne call site the analysis could not pin to a method.\n\nfield \ncallee the name being called, as far as the IR knows it\nsignature what it was called with — the widened argument types\nwhy :dynamic, :ambiguous, :maxdepth, :splat or :nomethod\nfile, line where to go and look\nwithin the method the call site is in\ncandidates the marked methods this site could reach, if any are visible\n\n\"Cannot tell\" is only actionable if the reader can go and look, which is what file/line are for. candidates is the difference between \"some call here is dynamic\" and \"this call could go to k(::KB), which is marked\".\n\n\n\n\n\n","title":"ExperimentalAPI.Unresolved"},{"category":"method","location":"api-runtime/#ExperimentalAPI.combine-Tuple{Symbol, Symbol}","page":"Observing and analysing","text":"combine(a::Symbol, b::Symbol) -> Symbol\n\nFold two verdicts into one: :clean < :unknown < :depends.\n\nNeeded because reach on a module folds one answer per public entry point into one answer for the module. Commutative and associative, so the order the entry points come back in cannot change the result.\n\n\n\n\n\n","title":"ExperimentalAPI.combine"},{"category":"method","location":"api-runtime/#ExperimentalAPI.dependents-Tuple{Module, Symbol}","page":"Observing and analysing","text":"dependents(m::Module, name::Symbol; kwargs...) -> Vector{Symbol}\n\nThe public names of m whose call graph reaches name.\n\nPropagation read backwards, which is the direction the question is actually asked in: a mark gets deleted because somebody looked at the definition, not at who reaches it. Compare reach(m; ignore = [name]) to see what removing it would change.\n\n\n\n\n\n","title":"ExperimentalAPI.dependents"},{"category":"method","location":"api-runtime/#ExperimentalAPI.isclean-Tuple{ExperimentalAPI.Reach}","page":"Observing and analysing","text":"isclean(r::Reach) -> Bool\n\nWhether verdict is :clean. false for :unknown as well as for :depends — the predicate answers \"may I rely on this\", and the honest non-answer is not a yes.\n\n\n\n\n\n","title":"ExperimentalAPI.isclean"},{"category":"method","location":"api-runtime/#ExperimentalAPI.reach-Tuple{Any, Type}","page":"Observing and analysing","text":"reach(f, types::Type{<:Tuple}; maxdepth = 32, ignore = Symbol[]) -> Reach\nreach(m::Module; kwargs...) -> Reach\n\nReport whether calling f with types can reach anything declared @experimental — including through callers that never name it.\n\nr = reach(analyse, Tuple{Model,Float64})\nverdict(r) === :clean || error(\"depends on: \", [x.mark.name for x in r.reached])\n\nThe module form folds every public entry point of m into one answer and reports which of them are affected in affected_entries: function-by-function does not scale to a package.\n\nignore names marks to treat as absent, which answers \"what would removing this mark change?\" without removing it. maxdepth bounds the walk and maxcandidates bounds how many methods a single ambiguous call site is willing to check; hitting either bound is reported as :unknown, never as :clean.\n\nWhat it can and cannot resolve\n\ncall site \na named call, however deep resolved\na function passed as a value resolved — Julia specialises on typeof(f)\n@nospecialized callee, called with a concrete function resolved\ninvoke(f, Tuple{Integer}, x) resolved to the method invoke pins, not the one dispatch would pick\na Union- or abstract-typed argument with methods on both sides :unknown — no unique method\na callee read out of a field or a table :unknown\na marked const or struct used in the body :depends\n\nA more specific unmarked method shadowing a marked one is resolved as what actually runs: Int goes to more_specific(::Int) and is clean, while UInt8 falls through to the marked ::Integer and is not.\n\nwarning: It answers about code, not about a run\nThis is static: it says what could be reached. entered and record say what was. A path this reports is not necessarily taken, and a :clean here is only as good as the call graph being closed — which is what :unknown exists to admit.\n\n\n\n\n\n","title":"ExperimentalAPI.reach"},{"category":"method","location":"api-runtime/#ExperimentalAPI.reach_script-Tuple{AbstractString}","page":"Observing and analysing","text":"reach_script(path::AbstractString; kwargs...) -> Reach\n\nThe same analysis for a script — a file that produces a figure rather than a package.\n\nTop-level declarations that cannot live inside a function (using, import, module, const, type definitions) are evaluated in a scratch module, because the analysis has to resolve the names the script uses; everything else is analysed as one thunk. So this loads the script's dependencies, and a script whose top level has side effects will have them.\n\n\n\n\n\n","title":"ExperimentalAPI.reach_script"},{"category":"method","location":"api-runtime/#ExperimentalAPI.verdict-Tuple{ExperimentalAPI.Reach}","page":"Observing and analysing","text":"verdict(r::Reach) -> Symbol\n\n:depends, :unknown or :clean, derived from what reach found.\n\n:depends — a marked definition is reachable. Proved, not suspected.\n:clean — the whole call graph was resolved and nothing marked is in it.\n:unknown — at least one call site could not be pinned to a method, and nothing marked was proved reachable through the rest.\n\n:depends wins over :unknown: an unresolvable call elsewhere does not make a proved dependency less proved. :unknown wins over :clean, which is the whole point.\n\n\n\n\n\n","title":"ExperimentalAPI.verdict"},{"category":"section","location":"observing/#Observing","page":"Observing","text":"A docstring can say a name is unfinished. It cannot tell you that this run went through it.\n\nThat is the question behind the mark: not \"is energy experimental\", which the author already knows, but \"did the number in this figure come out of code nobody validated\" — asked after the run, about the run, by somebody who may not have written either.","title":"Observing"},{"category":"section","location":"observing/#Without-asking","page":"Observing","text":"using ExperimentalAPI\n\n@experimental \"convergence not established below β ≈ 0.1\" energy(β) = β * 1.0000001\n\nenergy(0.5)\n\n$ julia sweep.jl\n┌ ExperimentalAPI: this run entered 1 experimental definition\n│   Main.energy — convergence not established below β ≈ 0.1\n└ set ENV[\"EXPERIMENTALAPI_SUMMARY\"] = \"0\" before `using` to silence this\n\nThree properties, each of them a decision:\n\nOn by default. The user who never asks is the one who needs telling. detecting reports whether the hook is armed; the environment variable has to be set before using ExperimentalAPI, because that is when atexit is registered.\nSilent unless something was entered. Loading a package that has marks prints nothing. A package that cannot be quiet is one people vendor around.\nCarries the reason. The name says which line to open; the reason says whether the result is affected.","title":"Without asking"},{"category":"section","location":"observing/#As-data","page":"Observing","text":"entered returns the same thing the summary prints, as a Vector{Entry}:\n\njulia> ExperimentalAPI.entered()\n1-element Vector{ExperimentalAPI.Entry}:\n Entry(Main.energy, \"convergence not established below β ≈ 0.1\")\n\nThe display above is a transcript rather than a doctest on purpose: Entry prints its module, and Documenter evaluates doctests in a sandbox whose module does not print as Main, so a doctest here would show a line no reader ever sees at their own REPL. The fields it is read for do not depend on where it ran, so those are checked:\n\njulia> using ExperimentalAPI\n\njulia> @experimental \"convergence not established below β ≈ 0.1\" energy(β) = β * 1.0000001\n\njulia> energy(0.5);\n\njulia> only(ExperimentalAPI.entered()).name\n:energy\n\njulia> only(ExperimentalAPI.entered()).reason\n\"convergence not established below β ≈ 0.1\"\n\njulia> only(ExperimentalAPI.entered()).count === nothing\ntrue\n\nA marked definition the run never entered is absent, not reported with a count of zero — the difference between \"observed\" and \"enumerated\".\n\nmarked_modules is the search this uses: the loaded modules that carry marks, found by walking rather than by a registry inside this package, because a table here would be written while the marked package is precompiled and so would be missing from its cache image. summary_text is what the exit hook prints, available as a string for a report of your own.","title":"As data"},{"category":"section","location":"observing/#What-it-costs","page":"Observing","text":"One short-circuit read in the body, and a write on the first call only.\n\nemitted into the body 1 thread 8 threads counts correctly?\nnothing 1.00× 1.00× —\nthe flag @experimental emits 1.03× 0.985× yes\na counter, plain shared Ref 1.03× 3.76× no — 40% lost to races\na counter, global atomic 1.17× 4.87× yes\na counter, per-thread atomic 1.12× 2.79× yes\n@warn, guarded so it fires once 5.65× — yes\n@warn maxlog=1 59.57× — yes\n\n10M calls of sqrt(abs(sin(x)cos(x) + exp(-|x|/1e6))), minimum of 7–9 trials, Julia 1.12.2.\n\nTwo of those rows decided the design. A flag written once and only read afterwards stops dirtying the cache line, which is why it is free at eight threads while every counting scheme is not. And the guarded @warn costs 5.65× even though it fires once: what stops the definition inlining is the call being in the body at all, not the warning being printed. That is why the notice is a summary at exit rather than a warning at the call.","title":"What it costs"},{"category":"section","location":"observing/#What-the-default-layer-does-not-answer","page":"Observing","text":"How often. Presence only — Entry.count is always nothing.\nWhich method. Flags are name-keyed, so two methods of a marked name share one.\nWhich call site, or by what path.\nAnything about a declaration-only mark. A name list, a struct, a const, a module: recorded and audited, never observed. See @experimental for the table.\n\nThe first three are record's, and it is a call rather than a default because of the table above.","title":"What the default layer does not answer"},{"category":"section","location":"observing/#Recording:-counts,-paths-and-time","page":"Observing","text":"r = record() do\n    simulate(model; steps = 10_000)\nend\n\njulia> r\nRecord — 1 marked definition entered in 0.42s\n  MyModel.energy  ×10000 — convergence not established below β ≈ 0.1\n     inclusive 0.31s   exclusive 0.28s\n     via record → sweep → step → energy\n  recorder overhead ≈ 4.1%\n\nrecord returns a Vector-like of Hit, so isempty(r) and r[1].count read the way they look — with the properties an empty vector could not carry:\n\nproperty why it is not just a vector\nenabled an empty record means \"nothing was entered\"; without this it is indistinguishable from \"nothing was recorded\", and those are opposite statements\nslots thread slots the counters were sized for, at least Threads.maxthreadid() — the interactive pool means a task's thread id can exceed nthreads()\noverhead the recorder's estimated share of the elapsed time, from a calibrated per-hit cost\nversions energy being experimental in v0.3 says nothing about v0.9","title":"Recording: counts, paths and time"},{"category":"section","location":"observing/#How-it-counts-without-a-counter-in-the-body","page":"Observing","text":"The emitted statement never changes. Opening a block clears every probe's flag, so the short-circuit fails and the write side runs on every call — and the write side is a function call, not an inlined store, so it can afford to count. Counts are therefore exact and survive inlining, which is what ruled out the sampling route: a definition small enough to be worth marking is small enough to be inlined, and a sampler has no frame left to attribute to.\n\nCounts are exact under threads too: per-thread counters, sized by maxthreadid() and padded so two threads never share a cache line, summed at the end.\n\npaths is a bounded sample rather than a complete list — a backtrace costs microseconds, so the recorder stops looking once it has seen enough. The paths a marked definition is reached by are few and repeat.","title":"How it counts without a counter in the body"},{"category":"section","location":"observing/#Time","page":"Observing","text":"inclusive and exclusive come from Julia's sampling profiler, through a package extension: without using Profile they are missing, which is not zero. A run that nobody timed has no fraction to report, and 0.0 would say the opposite.\n\nexperimental_fraction is the share of the run spent inside marked code, derived from the inclusive times — so it is time and not calls. One entry into a marked kernel that runs for a minute matters more than a million into a marked accessor.\n\nattribute does the same for a profile buffer that already exists, which is the twelve-hour-run case: a job that was already profiled must not have to be run again. What comes back is Attribution — samples, never calls, because a sampling profiler cannot count entries and a field called count holding a sample total would read as a measurement it did not make.","title":"Time"},{"category":"section","location":"observing/#As-a-gate,-and-as-evidence","page":"Observing","text":"assert_clean turns a record into a refusal:\n\nassert_clean() do\n    publish(compute(model))\nend\n\nwrite_record and stamp write it down instead. Both produce plain TOML, because a year later the package that made the figure may not resolve — and a provenance record nobody can open is not one.","title":"As a gate, and as evidence"},{"category":"section","location":"analysing/#Analysing","page":"Analysing","text":"entered and record say what a run did. reach says what a caller could do — before running it, and including through code that never names the marked thing.\n\njulia> r = reach(analyse, Tuple{Model,Float64});\n\njulia> verdict(r)\n:depends\n\njulia> r.reached\n1-element Vector{ExperimentalAPI.Reached}:\n Reached(MyModel.energy via analyse → sweep → inner → energy)\n\nThe model is Lean's sorry: a proof that uses one is not a proof, however many layers down it sits. Julia's call graph is not closed, though, so the answer has to be three-valued.","title":"Analysing"},{"category":"section","location":"analysing/#Three-answers,-and-why-the-third-one-exists","page":"Analysing","text":"verdict \n:depends a marked definition is reachable. Proved, not suspected\n:clean the whole call graph was resolved and nothing marked is in it\n:unknown at least one call site could not be pinned to a method\n\n:unknown is the point of the design. Two shapes really can reach a marked function while being statically invisible:\n\nstruct Holder; f::Function; end\ntop_field(h::Holder, x) = h.f(x)          # the callee is a value chosen at run time\n\nconst TABLE = Function[unstable, solid]\ntop_table(i, x) = TABLE[i](x)             # …and so is this one\n\nReporting :clean there is not a weaker claim, it is a false one. Every unresolved site comes back as an Unresolved carrying the file, the line, why it could not be resolved, and — when they are visible — the marked methods it could have reached.\n\nThere is deliberately no verdict field: a stored one makes :clean with a non-empty unresolved representable, and that is the single state this analysis must never report. verdict derives it, the way isbreaking derives its answer from a Diff. combine folds two verdicts (:clean < :unknown < :depends), which is what reach(::Module) does over a module's entry points.","title":"Three answers, and why the third one exists"},{"category":"section","location":"analysing/#What-it-resolves","page":"Analysing","text":"The walk is over inferred, un-optimised IR. Inference runs before inlining, so every call is still a call and every argument still has a type; optimize = true would show mul_float and find nothing.\n\ncall site \na named call, however deep resolved\na function passed as a value resolved — Julia specialises on typeof(f)\na @nospecialized callee, called with a concrete function resolved\ninvoke(f, Tuple{Integer}, x) resolved to the method invoke pins, not the one dispatch would pick\na marked const or struct used in the body :depends — a const is not a call site, and reading globals out of the IR is how it is seen\na Union- or abstract-typed argument whose candidates include a marked method :unknown\na callee read out of a field or a table :unknown\n\nA call site with several matching methods is not automatically unresolved: every candidate is walked, and if none of them reaches anything marked the site is resolved after all. That is not a guess — it is having checked all of them. Without it, convert(::Type, ::UInt32) (dozens of matching methods, none of them anybody's research code) would make every caller that formats a string :unknown.\n\nA more specific unmarked method shadowing a marked one is resolved as what actually runs: an Int goes to more_specific(::Int) and is clean, while a UInt8 falls through to the marked ::Integer method and is not.","title":"What it resolves"},{"category":"section","location":"analysing/#Whole-modules,-and-scripts","page":"Analysing","text":"r = reach(MyPackage)\nverdict(r)                # one answer for the package\nr.affected_entries        # …and which public entry points are not clean\n\nFunction-by-function does not scale to a package, and \"something in here is experimental\" is not actionable. Each entry point gets its own walk: sharing one visited set would make the second entry that reaches a mark through an already-walked callee look clean.\n\nreach_script is the shape a researcher actually has — a file that produces a figure, not a package. Note what it costs: the script's top-level using, const and type definitions are evaluated in a scratch module, because the analysis has to resolve the names the script uses.","title":"Whole modules, and scripts"},{"category":"section","location":"analysing/#The-exit,-read-backwards","page":"Analysing","text":"dependents(MyPackage, :energy)                  # who reaches it\nverdict(reach(MyPackage; ignore = [:energy]))   # what removing the mark would change\n\nignore answers \"what would removing this mark change?\" without removing it. dependents is propagation read the other way: a mark gets deleted because somebody looked at the definition, not at who reaches it.","title":"The exit, read backwards"},{"category":"section","location":"analysing/#What-it-is-not","page":"Analysing","text":"Not a run. It says what could be reached; record says what was. A path this reports is not necessarily taken.\nNot sound past a dynamic call. That is what :unknown is for, and why isclean answers false for it — the predicate means \"may I rely on this\", and the honest non-answer is not a yes.\nNot free. It runs inference over the call graph. maxdepth and maxcandidates bound it, and hitting either bound is reported as :unknown, never as :clean.","title":"What it is not"},{"category":"section","location":"releases/#Release-decisions","page":"Release decisions","text":"The payoff for marking anything at all: \"changing an experimental name is not breaking\" stops being an argument in a pull request and becomes a function call.","title":"Release decisions"},{"category":"section","location":"releases/#Snapshot,-then-compare","page":"Release decisions","text":"At release time, write down what the package promises and commit it:\n\nExperimentalAPI.write_snapshot(\"api.toml\", MyPackage)\n\nmodule = \"MyPackage\"\nversion = \"0.4.2\"\nstable = [\"adapt\", \"measure\"]\n\n[experimental.render_report]\nreason = \"reads Test's internal result tree\"\ntracking = \"https://github.com/org/MyPackage.jl/issues/12\"\n\nOn the next release, compare:\n\nd = compare(read_snapshot(\"api.toml\"), MyPackage)\nisbreaking(d) && error(\"breaking: $(d.removed_stable) removed, $(d.demoted) demoted\")","title":"Snapshot, then compare"},{"category":"section","location":"releases/#What-counts-as-breaking","page":"Release decisions","text":"move breaking? \nremoved_stable yes a settled name is gone\ndemoted yes a settled name is now called experimental\nremoved_experimental no this is what marking a name buys\npromoted no experimental → settled; the direction all of this is for\nadded_stable, added_experimental no \n\nRemoving a name you had declared experimental lands in removed_experimental and does not make the release breaking. That is the whole contract, and it is worth being precise about why it is legitimate: the notice was given in the source, at the definition, before the removal, in a form a downstream author's own test suite could have read. It is not a retroactive excuse.\n\ndemoted is the mirror image and is counted as breaking for the same reason. Taking a name you shipped as settled and calling it experimental afterwards does not un-promise it; it changes what callers were told after they believed it.","title":"What counts as breaking"},{"category":"section","location":"releases/#Names,-not-signatures","page":"Release decisions","text":"warning: Warning\ncompare reads name sets. A name present in both snapshots whose arguments, keyword arguments or return type changed is a breaking change, and this cannot see it. Read the diff as a floor on breakage, never as a clearance.\n\nThis is not a defect to be fixed later by making the snapshot signature-aware — or at least, not only that. Signature stability is a strictly harder question (a method table is not a set of names, and \"compatible\" is not a set operation), and a tool that answered it approximately would be worse than one that declines: a green light nobody should have trusted.\n\nWhich is also why the snapshot layer of this package is itself declared @experimental. The file format above was a guess, and making it signature-aware changed it.","title":"Names, not signatures"},{"category":"section","location":"releases/#The-method-level-half","page":"Release decisions","text":"The snapshot carries two more keys, and compare_methods reads them:\n\nstable_methods = [\"adapt(::Model, ::Grid)\", \"measure(::Model)\"]\n\n[experimental_methods.\"fetch_value(::Heisenberg, ::Energy)\"]\nreason = \"extrapolated from a finite-size sweep; no reference value\"\n\nThe key is a signature a human can read in a committed file, and it carries the argument types and the keyword names — so a method whose arguments or keywords changed reads as one key removed and another added, and isbreaking says so. That closes most of the blind spot above. What stays open is stated as a constant rather than left to be discovered: compare_methods_sees_keywords is true for names and the docstring says defaults are invisible, because a default lives in the body and moves nothing.\n\nNote also what stable does not do: a name with four methods of which one is marked stays in the covenant. Declaring one dispatch path unsettled is not a licence to remove the name.","title":"The method-level half"},{"category":"section","location":"releases/#When-a-mark-may-go","page":"Release decisions","text":"A mark that can only ever be added is a decoration. until= is what makes it a work item:\n\n@experimental(\n    \"no reference value yet\",\n    since = v\"0.1.0\",\n    tracking = \"https://github.com/org/Pkg.jl/issues/12\",\n    until = () -> isfile(joinpath(@__DIR__, \"..\", \"test\", \"refs\", \"energy.toml\")),\n    energy(β) = 2β,\n)\n\nready_to_promote calls that predicate; promotable lists the marks whose exit condition is met. A mark with no until= is false always — reporting it \"ready\" because it happens to be covered by a test would be inventing a criterion the author did not state — and those are reported separately by marks_without_exit, which is the different finding they are.\n\nage reads since on the axis a version bump is breaking along, and stale_since is the list a CI job turns into a nag. exceeds_mark_cap is the ratchet, in the shape test_surface's skip already has.","title":"When a mark may go"},{"category":"section","location":"releases/#Provenance-next-to-the-result","page":"Release decisions","text":"stamp(\"figures/energy_sweep.provenance.toml\") do\n    sweep(model; βs = 0.05:0.05:2.0)\nend\n\nThe end state this is all for: a figure's directory says which unvalidated code paths produced it, in plain TOML with the reason, the counts and stamp_versions beside them. A year later the package may not resolve, and a provenance record nobody can open is not one.","title":"Provenance next to the result"},{"category":"section","location":"checking/#Checking","page":"Checking","text":"This is the part the rest of the package exists for.","title":"Checking"},{"category":"section","location":"checking/#The-audit","page":"Checking","text":"audit takes the public surface — names(m), which since Julia 1.11 is exactly \"exported or public\" — and sorts every name into one bucket:\n\nbucket meaning\ndocumented has a docstring\ndeclared has an @experimental mark — an additional account, never a substitute\nforeign public here, but bound in another package — not this module's to account for\nundocumented no docstring, marked or not — the finding test_surface asserts empty\nunaccounted neither account at all — a subset of undocumented, and the worst case\ndangling marked, but not public — the module contradicting itself\n\nA mark says the shape is unsettled. That is never a reason to say nothing about what the name does, so a marked name with no prose is a finding exactly as an unmarked one is.\n\njulia> audit(Archeion)\nPublic surface of Archeion — 36 names\n  documented      33\n  experimental     0\n  undocumented     3   ← no docstring\n     FTPSTransport, pull_file, push_dir\n\nThe list is never truncated. A coverage report that elides its tail reads as if the tail were empty.","title":"The audit"},{"category":"section","location":"checking/#As-a-test","page":"Checking","text":"using MyPackage, ExperimentalAPI, Test\n\nExperimentalAPI.test_surface(MyPackage)\n\ntest_surface lives in a package extension, so it appears once Test is loaded and ExperimentalAPI never becomes a run-time dependency on Test for your users.\n\nIt emits one @testset per finding, so a failing CI log names the symbol in its header rather than printing a set difference somebody has to diff by eye:\n\npublic surface of MyPackage: Test Failed\n  pull_file is documented or @experimental\n  push_dir is documented or @experimental\n\nIt also fails on dangling marks, and it returns the Audit on the normal return path whether it passed or not — so a caller that wants the numbers does not have to run the check twice:\n\na = ExperimentalAPI.test_surface(MyPackage)\n@info \"surface\" total = length(a.surface) experimental = length(a.declared)\n\noutputlevel = 1 additionally prints the audit. Nothing else depends on it.","title":"As a test"},{"category":"section","location":"checking/#What-it-cannot-see","page":"Checking","text":"Stated here rather than in a footnote, because a check whose blind spots go unstated reads as if it had none:\n\nSignatures. A public name whose arguments, keywords or return type change under it is invisible. Marking that name experimental is the only way to give notice, and the audit cannot tell you that you should have.\nProse quality. A docstring reading TODO counts as documented. isdocumented asks whether prose exists, never whether it is any good.\nMethods added to another package's function are not in names(m) and never will be — which is why the audit has a second half. contributed_methods finds them, unaccounted_methods reports the ones with neither a docstring nor a mark, and test_surface asserts on them. For a package whose surface is such methods — fetch(model, quantity) with 570 of them — a clean name audit reports nothing while having looked at none of them.\nNames public only inside an extension, which is a separate module — audit it separately, or avoid the blind spot the way this package does: declare the function and its docstring in the parent (function test_surface end in src/ExperimentalAPI.jl) and let the extension add only the method. The name is then on the parent's surface and audit sees it.\nWhether a name appears in your guide, README or documentation site. Docstring presence and documentation-page presence are different sets, and this checks only the first. Documenter's checkdocs = :public and a @autodocs block cover part of the second.","title":"What it cannot see"},{"category":"section","location":"checking/#Combining-with-Documenter","page":"Checking","text":"The two checks are complementary and neither subsumes the other:\n\nmakedocs(; modules = [MyPackage], checkdocs = :public)   # every public name has a docstring\n\nDocumenter's checkdocs = :public fails a build when a public name has no docstring, and test_surface requires the same thing — the two agree, and running both is not a contradiction. What test_surface adds is foreign (a re-exported name whose prose is somebody else's job) and dangling (a mark on a name that was never made public), neither of which Documenter has a notion of.\n\nRun both. Neither is a looser version of the other, and aqua_compatible_names computes the difference between them so a project running both can see exactly which names it would have to argue about. Empty means the two agree.","title":"Combining with Documenter"},{"category":"section","location":"checking/#The-method-level-half","page":"Checking","text":"julia> audit(Downstream).contributed_methods\n4-element Vector{Method}:\n fetch_value(::Ising, ::Energy) …\n ⋮\n\njulia> unaccounted_methods(Downstream)      # neither a docstring nor a mark\n2-element Vector{Method}:\n ⋮\n\nDocstrings are keyed by signature, so isdocumented(::Method) is a real question and not the same one as isdocumented(m, :name). Only the module that wrote the method is asked: the generic's own docstring upstream has the key Tuple{Any, Any}, and letting it count would make one docstring account for all 570 methods anybody ever contributed.\n\ntest_surface asserts on these with require_methods = :foreign by default — methods on another package's generic, not on Base's. Base.show(io, ::Audit) implements a protocol whose documentation is Base's, and a default that reported every show, == and getindex method as a finding is a default that gets switched off wholesale. extends_base is the rule, stated as \"who owns the generic\" rather than as a list of interface functions — a list is not closed under the ones Julia adds next. require_methods = :all widens it.","title":"The method-level half"},{"category":"section","location":"checking/#How-well-is-any-of-it-exercised?","page":"Checking","text":"The mark records where it was written, and --code-coverage records a count per line. Joining the two answers the worst case a marked definition can be in:\n\njulia> unverified(MyPackage)          # marked AND never executed by the suite\n1-element Vector{ExperimentalAPI.Mark}:\n Mark(MyPackage.never_called, \"shipped without ever being called\")\n\njulia> coverage(MyPackage, :half_exercised)\n0.6\n\ncoverage answers missing, never 0.0, when the run has no coverage data: a run without --code-coverage has nothing to say, and reporting zero would flag every marked definition in every ordinary run. The counters are flushed from the running process rather than read from the files Julia writes at exit, because a test that has to wait for the process to end cannot assert anything.\n\nstale_marks is the check that keeps the join honest: an edit above a definition moves the code and not the record, and every downstream reader of mk.line then describes the wrong lines silently.","title":"How well is any of it exercised?"},{"category":"section","location":"#ExperimentalAPI.jl","page":"Home","text":"public says who may call a name. Nothing says whether the name is finished — and nothing tells you, after a twelve-hour run, that the number you are about to publish came out of code its author had not validated.\n\n@experimental is that mark, written at the definition site where the author is, and readable by a machine so the answer arrives without anyone remembering to ask for it.\n\nusing ExperimentalAPI\n\n@experimental \"convergence not established below β ≈ 0.1\" energy(β) = β * 1.0000001\n\nenergy(0.5)\n\n$ julia sweep.jl\n… your output …\n┌ ExperimentalAPI: this run entered 1 experimental definition\n│   MyModel.energy — convergence not established below β ≈ 0.1\n└ set ENV[\"EXPERIMENTALAPI_SUMMARY\"] = \"0\" before `using` to silence this\n\nNobody asked for that summary. It is on by default, it carries the reason rather than just the symbol, and a marked definition the run never entered is absent — not reported with a count of zero. The same answer is available programmatically through entered.","title":"ExperimentalAPI.jl"},{"category":"section","location":"#Five-questions,-and-the-first-is-the-reason-to-have-this","page":"Home","text":"question the call\ndid this run go through unvalidated code? entered — and the summary at exit says so anyway\nhow often, by which paths, and how much of the run? record\ndoes this caller depend on something unvalidated, without naming it? reach\nwhat is unfinished here, and which public names are undescribed? experimental, audit\nis dropping this name breaking? compare, compare_methods\n\nA docstring can carry the fourth row. Nothing a human writes can carry the first: the question is not \"is this name experimental\" but \"did this run go through one\", and it has to be answered after the run, about the run.","title":"Five questions, and the first is the reason to have this"},{"category":"section","location":"#What-it-costs","page":"Home","text":"One short-circuit read in the body, and a write on the first call:\n\nemitted into the body 1 thread 8 threads\nnothing 1.00× 1.00×\nthe flag @experimental emits 1.03× 0.985×\na counter, plain shared Ref 1.03× 3.76× — and loses 40% of its increments to races\na counter, global atomic 1.17× 4.87×\n@warn, guarded so it fires once 5.65× —\n\n10M calls of sqrt(abs(sin(x)cos(x) + exp(-|x|/1e6))), minimum of 7–9 trials, Julia 1.12.2. The flag is written once and only read afterwards, so it stops dirtying the cache line — which is why it is free at eight threads while every counting scheme is not, and why the notice is a summary at exit rather than a warning at the call.\n\nOnly a definition with a body carries a flag; see @experimental for the form-by-form table.\n\nCounting, call sites and paths are a separate layer, and it is opt-in for exactly the reason the table gives. record reaches the same statement from the other side: opening a block clears every flag, so the short-circuit fails and the write side — a function call, not an inlined store — does the counting. Nothing in a marked body changes; what changes is which branch of the one statement is taken. Counts are exact and survive inlining, which is why they come from a counter and not from a sampler.","title":"What it costs"},{"category":"section","location":"#Why-this-is-a-separate-axis","page":"Home","text":"Visibility was solved by the language: Julia 1.11's public keyword decides who is allowed to call a name, and names(m) reports the result. Stability is the orthogonal question — whether the name is finished — and it has no keyword, no query, and no check.\n\nThe two are genuinely independent. A name can be:\n\npublic and settled — the normal case, and the one a docstring describes;\npublic and unfinished — real, common, and currently expressible only as prose;\nprivate and settled, or private and unfinished — not this package's business either way, because nobody was promised anything.\n\nThe second row is what @experimental is for.","title":"Why this is a separate axis"},{"category":"section","location":"#Why-this-cannot-be-a-feature-of-Aqua","page":"Home","text":"A mark is written in src/, on the line above the definition, so the marked package depends on whatever provides @experimental at run time. Aqua is a test-only dependency. It can own the check; it structurally cannot own the declaration.\n\nThe check is not the same set difference either. Docs.undocumented_names reports every public name without a docstring, including names re-exported from a dependency whose prose is somebody else's job. audit files those as foreign, and adds dangling — a mark on a name that was never made public, which is the module contradicting itself.","title":"Why this cannot be a feature of Aqua"},{"category":"section","location":"#What-this-is-not","page":"Home","text":"axis already solved by this package\nwho may call a name export, public (1.11) orthogonal\na name on its way out @deprecate opposite direction\ntype stability DispatchDoctor unrelated\nevery public name has a docstring Docs.undocumented_names, Aqua.test_undocumented_names the same requirement, not a looser one — plus foreign and dangling\ngenerating documentation Documenter only ever checks whether prose exists\nrun-time behaviour — one short-circuit read in the body — see the table above\nhow often a path ran Profile, @time answered by record, which is opt-in; the default layer knows whether, never how often\nwhat a caller depends on — reach, statically, with :unknown as an honest third answer\n\n@experimental adds one statement to the body and nothing else. It does not add a method, does not change dispatch, and never allocates or logs.","title":"What this is not"},{"category":"section","location":"#Install","page":"Home","text":"Not in the General registry yet — install by URL:\n\npkg> add https://github.com/QAtlasHub/ExperimentalAPI.jl\n\nIt is loaded by the package being marked, so it is a normal dependency — but it pulls in nothing beyond TOML, and Test only through a package extension, so a consumer of your package never loads Test because of this.","title":"Install"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Declaring — the forms @experimental accepts, and what it refuses\nObserving — what a run went through, and record for how often and how much\nAnalysing — what a caller could reach, and why the answer is three-valued\nChecking — the audit, its buckets, the method-level half, and what it cannot see\nRelease decisions — saying mechanically that a change is not breaking\nAdopting it — turning this on for a package that already has a backlog","title":"Where to go next"}]
}
